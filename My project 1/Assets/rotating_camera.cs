using System;
using System.Threading;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.UIElements;

public class rotating_camera : MonoBehaviour
{
    public float CurPosZ;
    private float minimum;
    private float maximum;  
    // Update is called once per frame
    void Update()
    {
        // if (Input.GetKeyDown(KeyCode.RightArrow))
        // {
        //     Transform.Parent(GameObject.Find("Player").transform, GameObject.Find("Player").transform.up, 100*Time.deltaTime);
        // }

        
        // //transform.position = transform.position + new Vector3(CurPosZ, 0, 0);
    }
}
